---
import { resume } from '../../profile/resume';
---
<ul class="space-y-4">
  {resume.awards.map((a) => (
    <li class="flex flex-wrap items-baseline justify-between gap-x-4 border-l-2 border-accent pl-4">
      <div>
        <h3 class="font-display text-lg font-semibold text-zinc-900 dark:text-zinc-50">{a.title}</h3>
        <p class="text-sm text-zinc-600 dark:text-zinc-400">{a.issuer}</p>
        {a.note && <p class="mt-1 text-sm italic text-zinc-500 dark:text-zinc-400">{a.note}</p>}
      </div>
      <span class="text-sm text-zinc-500 dark:text-zinc-400">{a.year}</span>
    </li>
  ))}
</ul>
