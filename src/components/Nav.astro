---
import ThemeToggle from './ThemeToggle.astro';
const path = Astro.url.pathname;
const isHome = path === '/';
const isBlog = path.startsWith('/blog') || path.startsWith('/tags') || path.startsWith('/years');
const isProjects = path.startsWith('/projects');
const isAbout = path.startsWith('/about');

const linkClass = (active: boolean) =>
  `relative text-xs sm:text-sm font-medium tracking-wide transition ${
    active
      ? 'text-zinc-900 dark:text-zinc-50'
      : 'text-zinc-500 hover:text-zinc-900 dark:text-zinc-400 dark:hover:text-zinc-50'
  }`;

const tabs = [
  { href: '/', label: 'Home', active: isHome },
  { href: '/blog', label: 'Blog', active: isBlog },
  { href: '/projects', label: 'Projects', active: isProjects },
  { href: '/about', label: 'About', active: isAbout },
];
---
<header class="sticky top-0 z-40 border-b border-zinc-200/60 bg-zinc-50/80 backdrop-blur dark:border-zinc-800/60 dark:bg-zinc-950/80">
  <div class="container-wide flex h-16 items-center justify-between">
    <a href="/" class="font-display text-base font-semibold tracking-tight text-zinc-900 dark:text-zinc-50 sm:text-lg">
      Mario&nbsp;Dias
    </a>
    <nav class="flex items-center gap-4 sm:gap-8">
      {tabs.map((t) => (
        <a href={t.href} class={linkClass(t.active)}>
          {t.label}
          {t.active && <span class="absolute -bottom-[21px] left-0 right-0 h-px bg-accent" />}
        </a>
      ))}
      <ThemeToggle />
    </nav>
  </div>
</header>
